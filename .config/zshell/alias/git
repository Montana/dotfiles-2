# git
alias newmain="git checkout temp; git branch -D main; git fetch; git checkout main"
alias pullmain="git checkout main; git pull origin main"

alias gs="~/.config/scripts/format-with-prettier.sh && clear && git status"

